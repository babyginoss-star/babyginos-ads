[build]
  functions = "netlify/functions"

# La programación (schedule) ya está declarada dentro de sync-ads.mjs
# con: export const config = { schedule: "0 11 * * *" }
# No hace falta repetirla acá.
